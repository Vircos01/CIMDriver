# Utility-referentie

Dit document beschrijft de utilitylaag van CIMDriver. In deze laag staat ondersteunende logica die door meerdere functionele delen van de app kan worden gebruikt, zoals classificatie, tijd, export, backup en berekeningen.

## Bestandsoverzicht

### `AddressMatching.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/util`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `BackupUtil.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/util`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `ExportUtil.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/util`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `TimeUtil.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/util`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `TripClassification.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/util`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `TripDistance.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/util`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `TripStatus.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/util`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `WorkHoursCalculator.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/util`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `WorkHoursNormalizer.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/util`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `WorkHoursUtil.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/util`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.
