# UI-referentie

Dit document beschrijft de gebruikersinterface van CIMDriver op scherm-, component- en navigatieniveau. De UI-laag is omvangrijk en bestaat uit schermen, componenten, dialogs, navigatieklassen, thema-onderdelen en ViewModels.

## Schermen

### `AboutScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `AddAddressScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `AddTripScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `AddVehicleScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `AddWorkDayScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `AddressBookScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `ComposeLocale.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `DashboardScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `DiagnosticsScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `HelpScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `Screens.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `SettingsScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `TripDetailScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `TripsScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `VehiclesScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `WorkHoursScreen.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/screens`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

## Componenten

### `AddTripComponents.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/components`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `Charts.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/components`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `PermissionComponents.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/components`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `SettingsComponents.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/components`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `SwipeToDeleteContainer.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/components`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `TripComponents.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/components`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `VehicleComponents.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/components`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

## Navigatie

### `AppNavigation.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/navigation`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `NavRoutes.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/navigation`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.

### `Navigation.kt`

- Bestand binnen `app/src/main/java/com/cimdriver/app/ui/navigation`.
- Opgenomen in de huidige codecatalogus en hoofdbronset van de applicatie.
